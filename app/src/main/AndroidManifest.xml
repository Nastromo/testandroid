<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.face_location.facelocation">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="com.google.android.providers.gsf.permission.READ_GSERVICES" />
    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality.
    -->
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".StartActivity"
            android:screenOrientation="portrait"
            android:theme="@style/GreyStatusBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".TermsOfUse"
            android:screenOrientation="portrait"
            android:theme="@style/GreyStatusBar" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/. 
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".Map"
            android:label="@string/title_activity_map"
            android:screenOrientation="portrait"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBar" />

        <!-- Return this block if fragments need for swiping, else delete before release -->
        <!--
           <activity
           android:name=".AddLocationFirstFragment"
           android:windowSoftInputMode="adjustResize"
           android:label="@string/title_activity_new_location"
           android:theme="@style/AppThemeScroll" />
        -->
        <!-- TODO android:windowSoftInputMode="adjustResize" - makes content jump under keyboard -->
        <activity android:name=".AddLocationFirstActivity" />
        <activity android:name=".AddLocationSecondActivity" />
        <activity android:name=".AddLocationThirdActivity" />
        <activity android:name=".AddLocationFourthActivity" />
        <activity android:name=".LogInActivity" />
        <activity android:name=".RegistrationFirstActivity" />
        <activity android:name=".RegistrationSecondActivity" />
        <activity android:name=".NewEventFirstActivity" />
        <activity android:name=".NewEventSecondActivity" />
        <activity android:name=".NewEventThirdActivity" />
        <activity android:name=".NewEventFourthActivity" />
        <activity android:name=".NewEventFifthActivity" />
        <activity android:name=".NewEventSixthActivity" />
        <activity android:name=".NewEventSeventhActivity" />
        <activity android:name=".NewEventEighthActivity" />
        <activity android:name=".NewEventNinthActivity" />
        <activity android:name=".SearchLocationActivity" />
        <activity android:name=".SupportActivity" />
        <activity
            android:name=".MyProfileActivity"
            android:windowSoftInputMode="adjustPan" />
        <activity
            android:name=".MyAppActionsActivity"
            android:label="@string/title_activity_my_app_actions"
            android:theme="@style/AppTheme"
            android:parentActivityName="com.face_location.facelocation.MainActivity" ></activity>
    </application>

</manifest>